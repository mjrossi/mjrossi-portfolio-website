node_modules/
dist/
.astro/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# lighthouse report output (lhci writes this wherever it runs)
.lighthouseci/

# vscode
.vscode/

# claude
.claude/

# design notes written while working a change out. Process artifacts, not
# documentation -- what they conclude belongs in CLAUDE.md or a header comment,
# which is where the next person will actually look for it.
docs/superpowers/

# mise local (committed: mise.toml, mise.ci.toml, mise.development.toml, mise.local.toml.example)
mise.local.toml
mise.*.local.toml
